"""PySide6 desktop UI for NetScope (Phase 1).

The UI consumes the exact same `Scanner` -> `AccessPoint` pipeline the CLI uses,
so it works against the mock backend (any OS / no hardware) and the real
Windows backend without changing a line of UI code.
"""
